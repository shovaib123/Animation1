import {S3Client,PutObjectCommand} from "@aws-sdk/client-s3";import {getSignedUrl} from "@aws-sdk/s3-request-presigner";
const s=()=>new S3Client({region:"auto",endpoint:`https://${process.env.R2_ACCOUNT_ID}.r2.cloudflarestorage.com`,credentials:{accessKeyId:process.env.R2_ACCESS_KEY_ID,secretAccessKey:process.env.R2_SECRET_ACCESS_KEY}});
export default async function(req,res){if(req.method!=="POST")return res.status(405).json({error:"POST only"});try{let{key,contentType}=req.body||{};if(!key?.startsWith("uploads/")||!contentType?.startsWith("video/"))return res.status(400).json({error:"Invalid video"});let uploadUrl=await getSignedUrl(s(),new PutObjectCommand({Bucket:process.env.R2_BUCKET_NAME,Key:key,ContentType:contentType}),{expiresIn:900});res.json({uploadUrl,key})}catch(e){res.status(500).json({error:e.message})}}
import {S3Client,GetObjectCommand} from "@aws-sdk/client-s3";const s=()=>new S3Client({region:"auto",endpoint:`https://${process.env.R2_ACCOUNT_ID}.r2.cloudflarestorage.com`,credentials:{accessKeyId:process.env.R2_ACCESS_KEY_ID,secretAccessKey:process.env.R2_SECRET_ACCESS_KEY}});
export default async function(req,res){if(req.method!=="GET")return res.status(405).json({error:"GET only"});let id=String(req.query.id||"");if(!/^[a-f0-9-]{36}$/.test(id))return res.status(400).json({error:"Invalid job"});try{let x=await s().send(new GetObjectCommand({Bucket:process.env.R2_BUCKET_NAME,Key:`jobs/${id}.json`}));res.setHeader("Cache-Control","no-store");res.json(JSON.parse(await x.Body.transformToString()))}catch(e){res.status(404).json({error:"Job not found"})}}
